# devops-netology

GITLAB



## .gitignore
A `.gitignore` file has been added to the `terraform` directory. It ignores:
- Terraform state files (`*.tfstate` and their backups)
- local provider cache and plugins (`.terraform/` directory)
- variable files containing secrets (`*.tfvars`)
- crash logs (`crash.log`)
